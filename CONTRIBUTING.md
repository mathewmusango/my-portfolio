# Contributing

This repository is a static portfolio site, plus the Terraform that provisions the infrastructure it runs on and the CI that deploys it.

It is maintained by one person and does not take outside contributions — changes are made through a pull request against `main`, where the shared checks run, the commit is signed and the merge is squashed.
