name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: Tag to create or refresh the release for (e.g., v1.0.0)
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - uses: ./.github/actions/build-site

      - name: Package site
        run: |
          cd site
          zip -r ../site.zip .
          cd ..

      - name: Generate SBOM (CycloneDX)
        run: |
          pip install cyclonedx-bom==7.3.1
          cyclonedx-py requirements -i requirements.txt -o sbom.cdx.json

      - name: Extract changelog notes
        env:
          TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
        run: |
          VERSION="${TAG#v}"
          sed -n "/^## \[${VERSION}\]/,/^## \[/p" CHANGELOG.md | sed '1d;$d' > release-notes.md
          if [ ! -s release-notes.md ]; then printf 'See CHANGELOG.md for details.\n' > release-notes.md; fi

      - name: Create or refresh GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag || github.ref_name }}
        run: |
          if gh release view "${TAG}" >/dev/null 2>&1; then
            gh release edit "${TAG}" --notes-file release-notes.md
            gh release upload "${TAG}" site.zip sbom.cdx.json --clobber
          else
            gh release create "${TAG}" site.zip sbom.cdx.json \
              --title "Release ${TAG}" \
              --notes-file release-notes.md
          fi
